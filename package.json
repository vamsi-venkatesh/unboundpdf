{
  "name": "unboundpdf-public",
  "version": "1.0.0",
  "private": true,
  "description": "Selected runnable source and verification evidence from UnboundPDF.",
  "scripts": {
    "serve": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.mjs",
    "audit:public": "node scripts/audit-public.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "playwright": "1.61.1"
  }
}
